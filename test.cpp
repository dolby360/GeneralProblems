#include "include/catch.hpp"
// #include "include/addTwoLinkedList.hpp"

// #ifdef DEBUG_ALL

// #endif


TEST_CASE("Dummy"){   
	// int arr1[] = { 1 , 8 };
	// int arr2[] = { 0 };
	// int size1 = sizeof(arr1) / sizeof(arr1[0]);
	// int size2 = sizeof(arr2) / sizeof(arr2[0]);
	// ListNode* ln1;
	// ListNode* ln2;
	// ListNode* head1;
	// ListNode* head2;

	// ln1 = new ListNode(arr1[0]);
	// head1 = ln1;
	// ln2 = new ListNode(arr2[0]);
	// head2 = ln2;
	// for (int i = 1; i < size1; i++) {
	// 	ln1->next = new ListNode(arr1[i]);
	// 	ln1 = ln1->next;
	// }
	// for (int i = 1; i < size2; i++) {
	// 	ln2->next = new ListNode(arr2[i]);
	// 	ln2 = ln2->next;
	// }
	// ln1->next = NULL;
	// ln2->next = NULL;
	// Solution sol;
	// ListNode* ln = sol.addTwoNumbers(head1, head2);
	// ListNode* expected;
	// int expArr[] = {1,8};
	// for(int i = 0; i < 2; i++)
	// 	expected = new ListNode();
	// }
 
    REQUIRE(
        1 == 1
    );
}

// #ifdef DEBUG_ALL

// #endif